import React from "react";
import ReactDOM from "react-dom/client";
import FestHelferApp from "./FestHelferApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <FestHelferApp />
  </React.StrictMode>
);